I implemented a red-black tree with fine-grained concurrency for
extra credit.

---------------------------------------------------------------------

To compile:

	Run 'make' in the directory containing Makefile.

To run:

	With specified input file:

		./concurrent_red_black_tree FILE

	With sample input file:

		./concurrent_red_black_tree
